{
  "name": "finalize-result",
  "version": "0.3.1",
  "description": "Persist a raw JSON payload to a local file. Used as the closing step of a task pipeline so downstream automation can consume structured results.",
  "private": true
}